# You can create the file local_config.properties to overwrite config.properties.

# Set the DBnary TDB directory here:
#wiktionaryTdbDirectory = <directory path>

# Wordnet Directory (the one containing .noun, .adv, .adj [...] files)
#wordnetPath = <directory path>