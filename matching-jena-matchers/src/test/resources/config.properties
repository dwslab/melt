# You can create the file local_config.properties to overwrite config.properties.

# Set the DBnary TDB directory here:
wiktionaryTdbDirectory = <directory path>

# Set the DBpedia TDB directory here:
dbpediaTdbDirectory = <directory path>

# Set the BabelNet TDB directory here:
babelnetTdbDirectory = <directory path>

# the following entry is only there for a unit test, please do not delete
testKey = myTest